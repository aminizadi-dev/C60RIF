@{
    const string prefix = "passenger-average-travel-length-by-agency-bar-chart";
    const string hideCardAttributeName = "Reports.HidePassengerAverageTravelLengthByAgencyBarChartCard";
    var hideCard = await genericAttributeService.GetAttributeAsync<bool>(await workContext.GetCurrentCustomerAsync(), hideCardAttributeName);
}

<script src="~/lib_npm/chart.js/chart.umd.min.js"></script>

<div class="card card-primary card-outline @if (hideCard){<text>collapsed-card</text>}" id="@(prefix)-card">
    <div class="card-header with-border">
        <h3 class="card-title">
            <i class="fas fa-chart-bar"></i>
            @T("Admin.Reports.Passengers.AverageTravelLengthByAgency")
        </h3>
        <div class="card-tools float-right">
            <div class="input-group input-group-sm" style="width: 220px;">
                <div class="input-group-prepend">
                    <span class="input-group-text">@T("Admin.Configuration.Cities")</span>
                </div>
                <select id="@(prefix)-city" class="form-control" @if (hideCard) { <text> disabled="disabled" </text> }></select>
            </div>
            <button type="button" class="btn btn-tool margin-l-10" data-card-widget="collapse">
                @if (hideCard)
                {
                    <text><i class="fas fa-plus"></i></text>
                }
                else
                {
                    <text><i class="fas fa-minus"></i></text>
                }
            </button>
        </div>
    </div>
    <div class="card-body">
        <div class="chart" style="height: 300px;">
            <canvas id="@(prefix)-chart" height="300"></canvas>
        </div>
    </div>
</div>

<script>
    $(function() {
        var selectedCityId;

        $('#@(prefix)-card').on('click', 'button[data-card-widget="collapse"]', function () {
            var collapsed = !$('#@(prefix)-card').hasClass('collapsed-card');
            saveUserPreferences('@(Url.Action("SavePreference", "Preferences"))', '@hideCardAttributeName', collapsed);
            
            if (!collapsed) {
                $('#@(prefix)-city').removeAttr('disabled');
                if (!selectedCityId) {
                    loadCitiesAndChart();
                }
            } else {
                $('#@(prefix)-city').attr('disabled', 'disabled');
            }
        });

        var barConfig = {
            type: 'bar',
            data: {
                labels: [],
                datasets: [{
                    label: "@T("Admin.Reports.Passengers.AverageTravelLengthByAgency.Label")",
                    data: [],
                    backgroundColor: 'rgba(54, 162, 235, 0.8)',
                    borderColor: 'rgba(54, 162, 235, 1)',
                    borderWidth: 1
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        display: false
                    },
                    tooltip: {
                        callbacks: {
                            label: function(context) {
                                var row = context.raw || {};
                                if (row.averageLabel) {
                                    return row.averageLabel;
                                }
                                return context.parsed.y + ' ' + '@T("Admin.Reports.Passengers.AverageTravelLengthByAgency.Days")';
                            }
                        }
                    }
                },
                scales: {
                    x: {
                        display: true,
                        grid: {
                            display: false
                        },
                        ticks: {
                            maxRotation: 45,
                            minRotation: 45
                        }
                    },
                    y: {
                        display: true,
                        beginAtZero: true,
                        grid: {
                            display: true,
                            color: 'rgba(0,0,0,.05)',
                            lineWidth: 1
                        }
                    }
                }
            }
        };

        function changeBarChartCity(cityId) {
            var labels = [];
            var values = [];

            $.ajax({
                cache: false,
                type: "GET",
                url: "@Url.Action("LoadPassengerAverageTravelLengthByAgency", "Home")",
                data: {
                    cityId: cityId
                },
                success: function (data) {
                    if (data && data.length > 0) {
                        for (var i = 0; i < data.length; i++) {
                            labels.push(data[i].agencyName);
                            values.push({
                                x: data[i].agencyName,
                                y: data[i].averageDays,
                                averageLabel: data[i].averageLabel
                            });
                        }
                    } else {
                        labels = [];
                        values = [];
                    }

                    if (!window.passengerAverageTravelLengthByAgencyBarChart) {
                        barConfig.data.labels = labels;
                        barConfig.data.datasets[0].data = values;
                        window.passengerAverageTravelLengthByAgencyBarChart = new Chart(
                            document.getElementById("@prefix-chart").getContext("2d"),
                            barConfig
                        );
                    } else {
                        window.passengerAverageTravelLengthByAgencyBarChart.config.data.labels = labels;
                        window.passengerAverageTravelLengthByAgencyBarChart.config.data.datasets[0].data = values;
                        window.passengerAverageTravelLengthByAgencyBarChart.update();
                    }
                }
            });
        }

        function loadCitiesAndChart() {
            $.ajax({
                cache: false,
                type: "GET",
                url: "@Url.Action("LoadPassengerDistributionCities", "Home")",
                success: function (data) {
                    var $select = $('#@(prefix)-city');
                    $select.empty();
                    $select.append($('<option></option>').attr('value', '0').text('@T("Admin.Common.Select")'));

                    if (data && data.length > 0) {
                        for (var i = 0; i < data.length; i++) {
                            $select.append($('<option></option>').attr('value', data[i].id).text(data[i].name));
                        }
                    }

                    var defaultCityId = 8;
                    if ($select.find('option[value="' + defaultCityId + '"]').length) {
                        $select.val(defaultCityId);
                    }

                    if ($select.val() && $select.val() !== '0') {
                        selectedCityId = parseInt($select.val());
                        changeBarChartCity(selectedCityId);
                    }
                }
            });
        }

        $('#@(prefix)-city').on('change', function () {
            var cityId = parseInt($(this).val());
            selectedCityId = cityId;
            if (cityId > 0) {
                changeBarChartCity(cityId);
            } else if (window.passengerAverageTravelLengthByAgencyBarChart) {
                window.passengerAverageTravelLengthByAgencyBarChart.config.data.labels = [];
                window.passengerAverageTravelLengthByAgencyBarChart.config.data.datasets[0].data = [];
                window.passengerAverageTravelLengthByAgencyBarChart.update();
            }
        });

        @if (!hideCard)
        {
            <text>
                loadCitiesAndChart();
            </text>
        }
    });
</script>
